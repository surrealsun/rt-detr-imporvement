base_configs:
  - ../default.yaml
  - ../datasets/aitod.yaml

project:
  output_dir: outputs/aitod_detail_fusion

model:
  use_detail_branch: true
  fusion_mode: learnable
  use_auxiliary_dense: false
